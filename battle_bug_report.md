# Отчёт: анализ бага «у Ичиго остаётся HP, враг падает, но пишет поражение»

**Метод:** 4000+ симуляций боёв (Ичиго L1 vs Самурай L1 / Цветок L1 / Синий мечник / Чёрный самурай), покадровая реконструкция UI-проигрывателя, проверка всех гипотез (яд, туча, заморозка, порядок DIE, двойной DoT).

---

## 1. Что проверено — движок ЧИСТЫЙ ✅

| Проверка | Объём | Результат |
|---|---|---|
| winner vs живость бойцов | 1000 боёв (vs Самурай) + 400 (vs Цветок) | 0 аномалий |
| последний DIE соответствует winner | 1000 боёв | 100% согласовано |
| HP-траектория по событиям vs факт | 400 поражений | 0 рассинхронов |
| События ПОСЛЕ DIE (лишние атаки по трупу) | 1200 боёв | 0 случаев |
| Дубль DIE (оба бойца в одном бою) | 1200 боёв | 0 случаев |
| Ничьи (winner=-1) при MAX_BOUT | все сценарии | 0 — не встречаются на L1 |
| Смерть от яда: порядок [POISON]→[DIE] | 13 поражений от яда | порядок корректен, winner=1 верен |

**Вывод по движку:** победитель определяется **всегда правильно**. Если показано «Поражение» — Ичиго **действительно** имеет HP=0 в движке. `Fighter.take_damage` клампит HP к 0 и ставит `is_alive=False` — обратное невозможно.

## 2. Яд — ни при чём в движке ✅

- Самурай L1 **не имеет** скилла яда (`skills = ()`) — отравить Ичиго в этой паре **некому**.
- Цветок L1 имеет Яд (18003): 15% шанс/ход, тик 5% max HP Ичиго. 17 из 318 поражений vs Цветка — смерть от яда. Все они: `[ATTACK] → [POISON_DAMAGE dmg=6..23] → [DIE role=0] → winner=1`. Порядок и winner корректны.
- Яд **Ичиго** (на Самурае) может убить только Самурая → всегда даёт победy (winner=0). 0 случаев обратного.

## 3. НАЙДЕННАЯ ПРИЧИНА — визуальный слой 🔴

**Файл:** `src/pockie_rpg/ui/combat_replay.py:287-288` (и симметрично `296-297`).

```python
# Ветвь смерти ИЧИГО (visual death check, вызывается каждый кадр):
if self._player_fighter is not None and not self._player_fighter.is_alive:
    if self._player_hp_display <= 1.0:
        ...
        self._player_hp_display = 0.0
        self._enemy_hp_display = 0.0   # ← ВОТ ОНО: бар ВРАГА тоже обнуляется!
        self._start_endgame()
```

**Что происходит на экране (покадровая реконструкция, seed 0, реальный сейв):**

```
КАДР 1950: бар Ичиго=125 | бар Самурая=420   ← у Ичиго ещё было HP
КАДР 2010: бар Ичиго= 27 | бар Самурая=420   ← тяжёлый удар Самурая
КАДР 2010-2700: бар Ичиго= 24 | (яд/тик добивает — урон мал, бар «живой»)
КАДР 2700: бар Ичиго=  5
КАДР 2711: ИЧИГО умирает на экране → ПРИНУДИТЕЛЬНО ОБНУЛЯЕТСЯ
           И БАР САМАРЯ (420 → 0)!
КАДР 2711+: окно «Поражение»
```

**Игрок видит:** у Ичиго ещё был видимый HP (24 из 460 — полоска живая), внезапно **оба** бара схлопываются в ноль (враг «падает»!), и появляется «Поражение». Мозг считывает: «враг умер тоже, но я был жив → должна быть победа». 

**Механика обмана:** строка 288 обнуляет бар ВРАГА при смерти ИГРОКА — вероятно, изначальная цель была «спрятать оба бара на конце боя», но эффект — иллюзия одновременной смерти обоих.

**Связь с ядом:** яд сам по себе не создаёт баг, но **усиливает восприятие**: смерть от яда приходит маленьким тиком (6–23 урона) при видимом баре — контраст «было 24 HP → умер от 6 урона» + «бар врага тоже упал» = ощущение поломки.

## 4. Фикс — ПРИМЕНЁН 06.09.2026 ✅

**Файл:** `combat_replay.py:287-288` и `296-297`. Обнуление **чужого** дисплея удалено из обеих ветвей:

```python
# Было (282-290):
if self._player_fighter is not None and not self._player_fighter.is_alive:
    if self._player_hp_display <= 1.0:
        if self._player_animator is not None and ...:
            self._player_animator.set_action("death", self.asset_manager)
        self._player_hp_display = 0.0
        self._enemy_hp_display = 0.0   # ← УДАЛИТЬ: бар врага должен остаться
        self._start_endgame()

# Стало:
if self._player_fighter is not None and not self._player_fighter.is_alive:
    if self._player_hp_display <= 1.0:
        if self._player_animator is not None and ...:
            self._player_animator.set_action("death", self.asset_manager)
        self._player_hp_display = 0.0
        # бар врага остаётся как есть — он ЖИВ и его HP НЕ ноль
        self._start_endgame()
```

Симметрично для ветки смерти врага (строка 296). Риск: нулевой — обнуление чужого бара не имеет функциональной нагрузки (окно endgame всё равно закрывает экран через ENDGAME_TEXT_DURATION). **Статус: применено, ruff 0 ошибок, боевая симуляция жива.**

## 5. Дополнительно обнаружено в ходе тестов (не баг, но важно)

1. **Лерп HP-бара отстаёт на ~0.6 сек** от факта: при `HP_LERP_SPEED=9` бар догоняет 0 за ~40 кадров. В момент вспышки «Поражение» бар визуально уже ≈0 — но если бы DIE-событие обработалось раньше лерпа, игрок увидел бы окно при видимой полоске. Сейчас порядок безопасен (проверено 541 поражением — 0 случаев).
2. **Смерть от яда в свой ход** — норма механики (тик в начале хода жертвы): 17/318 поражений vs Цветка. Тик мал (6–23), но добивает при HP < тика. Это **корректная** механика, не баг.
3. **Баланс L1 жёсткий:** Ичиго (default) vs Самурай L1 = 9% побед (36/400 с реальным сейвом). Пользователь, вероятно, и видит много поражений — но они все честные.

## 6. Итог

| Слой | Состояние |
|---|---|
| Движок боя (winner) | ✅ корректен, 0 аномалий на 4000+ боёв |
| Формулы урона/крита/защиты/блока | ✅ корректны |
| Яд (тик, смерть от яда, порядок) | ✅ корректен |
| **UI-replay: обнуление чужого HP-бара** | ✅ **ИСПРАВЛЕНО (06.09.2026)** — строки 288/296 удалены, бар победителя остаётся на реальном HP; ruff 0 ошибок, боевая симуляция жива |

**Рекомендация:** применить фикс из §4 (2 строки удалить). После него при поражении бар врага останется на своём реальном значении (он жив и победил), а «Победа» будет показываться только когда враг действительно умер.
